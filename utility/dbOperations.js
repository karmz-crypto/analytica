// db operations 

/* 1. populating model properties's object id with their refs
    2. creating documents -client -payment -purchase -sales -product 
    2. performing crud operations at model level and document level 
            eg : deleting all the transactions of the client along with their refs or deleting a single doc 
                of the client 
                updating follows the similar logic  */

const mongoose = require('mongoose');
